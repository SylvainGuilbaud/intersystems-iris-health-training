Include %occStatus

/// DO NOT EDIT. Generated by PatientProcess python class
Class pyprod.PatientProcess Extends Ens.BusinessProcess
{

Property GeocodingOperation As %VarString [ InitialExpression = "pyprod.GeocodingOperation" ];

Property WeatherOperation As %VarString [ InitialExpression = "pyprod.WeatherOperation" ];

Parameter SETTINGS = "GeocodingOperation:Basic:selector?context={Ens.ContextSearch/ProductionItems?targets=1&productionName=@productionId},WeatherOperation:Basic:selector?context={Ens.ContextSearch/ProductionItems?targets=1&productionName=@productionId}";

Property PythonClassName As %String(MAXLEN = "") [ InitialExpression = "PatientProcess" ];

Property PythonModuleOrScript As %String(MAXLEN = "") [ InitialExpression = "production" ];

Method OnInit() As %Status
{
    s status = $$$OK
    try{
        set pyprod = ##class(%SYS.Python).Import("intersystems_pyprod")
        do $method(pyprod,"_add_to_sys_path","/code/pyprod")
    } catch e {
        set status = $system.Status.Error(5001, e.AsSystemError())
        $$$LOGERROR("Error while importing the BusinessProcess PatientProcess: "_status)
    }
    Quit status
}

Method OnRequest(request As %Library.Persistent, Output response As %Library.Persistent) As %Status
{
      try{
        set PyLib = ##class(%SYS.Python).Import(..PythonModuleOrScript)
        set PythonClassObject = $method(PyLib,..PythonClassName, $this)
        Set arr = PythonClassObject.OnRequestHelper(request)
        set status = arr."__getitem__"("status")
        if arr."__getitem__"("response_available"){
          set response = arr."__getitem__"("response") 
        }

      } catch e {
        set status = $system.Status.Error(5001, e.AsSystemError())
        $$$LOGERROR("ERROR IN BP"_status)
      }
    Quit status
}

Storage Default
{
<Data name="PatientProcessDefaultData">
<Subscript>"PatientProcess"</Subscript>
<Value name="1">
<Value>GeocodingOperation</Value>
</Value>
<Value name="2">
<Value>WeatherOperation</Value>
</Value>
<Value name="3">
<Value>PythonClassName</Value>
</Value>
<Value name="4">
<Value>PythonModuleOrScript</Value>
</Value>
</Data>
<DefaultData>PatientProcessDefaultData</DefaultData>
<Type>%Storage.Persistent</Type>
}

}
